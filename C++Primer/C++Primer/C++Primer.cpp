// C++Primer.cpp : 定义控制台应用程序的入口点。
//

#include "stdafx.h"


int main()
{
	/*
	unsigned Int = 0;
	unsigned char Chr;
	cout << "输入无符号整形值：";
	cin >> Int;
	cout << endl << "输入无符号字符：";
	cin >> Chr;

	Sign test;
	Sign *t = new Sign;

	test.set(Int);
	test.set(Chr);
	cout << "调用对象的get方法输出a：" << test.get_a() << endl;
	cout << "调用对象的get方法输出b：" << test.get_b() << endl;

	cout << "使用new分配内存的调用方法：" << endl;

	t->set(Int);
	t->set(Chr);
	cout << "调用对象的get方法输出a：" << t->get_a() << endl;
	cout << "调用对象的get方法输出a：" << t->get_b() << endl;

	t->Squence();

	*/


	int month = 0;

	cin >> month;

	cout << month << endl;


	endl(cout << "\145");
	endl(cout << "\012");

    return 0;
}

